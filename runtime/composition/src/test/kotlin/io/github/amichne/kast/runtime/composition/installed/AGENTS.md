# Installed runtime composition tests

This directory owns installed-runtime protocol acceptance and focused topology-prerequisite proofs.
Keep tests on the public composition path, and preserve unavailable, stale, qualified, and complete
evidence as distinct typed outcomes.

Run `./gradlew :runtime:composition:test` after changing these tests.

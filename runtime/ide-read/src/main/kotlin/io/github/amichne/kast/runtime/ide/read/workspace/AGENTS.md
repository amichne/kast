# Hosted workspace inspection owner

This package owns KVP-028's `workspace.inspect` port over one retained existing IDE Project.

- Construction admits only `IDE_PROJECT`; an isolated-runtime candidate remains a closed misuse.
- A complete outcome requires an exact-root current `VfsPassiveReadCapability` observed from the
  retained Project immediately before projection.
- The route may observe current epoch state and project admitted root/capabilities only. It owns no
  transition, refresh, import, persistence, graph-build, process, source-write, or fallback port.
- Raw root text leaves only at `ProtocolText` projection. The opaque epoch is never serialized.

Run the graph-named negative and acceptance tasks, then `:runtime:ide-read:check`.

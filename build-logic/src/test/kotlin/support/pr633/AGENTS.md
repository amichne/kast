# PR 633 verification tests

This directory owns focused tests for the reusable PR 633 verification task types in
`build-logic/src/main/kotlin/support/pr633`.

Run `./gradlew -p build-logic test` after changing these proofs, then run the consuming root gate.

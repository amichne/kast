---
type: "Installation Guide"
title: "Install and connect Kast"
sidebarTitle: "Install and connect"
description: "Install Kast and make compiler-grounded Kotlin search available to an agent."
resource: "file://docs/public/start.mdx"
tags: ["installation", "agents", "Codex", "IntelliJ"]
timestamp: "2026-09-14T00:00:00-04:00"
code_sources:
  - path: "install.sh"
  - path: "gradle/libs.versions.toml"
  - path: "app-server/src/main/kotlin/io/github/amichne/kast/appserver/KastCodexMain.kt"
    symbols: ["KastCodexMain"]
  - path: "app-server/src/main/kotlin/io/github/amichne/kast/appserver/InstalledBrokerServer.kt"
    symbols: ["InstalledBrokerServerConfiguration"]
  - path: "app-server/src/main/kotlin/io/github/amichne/kast/appserver/host/InstalledCodexClientLauncher.kt"
    symbols: ["InstalledCodexClientLauncher"]
  - path: "app-server/src/main/kotlin/io/github/amichne/kast/appserver/PersistentBrokerService.kt"
    symbols: ["BrokerServiceLaunchCommand"]
icon: "rocket"
keywords: ["install", "Codex", "IntelliJ", "agent integration"]
---

Install the IDEA plugin, connect Codex, and find a declaration in your repository.

<Note>The Codex integration is a preview. See the [compatibility record](/reference/compatibility) for tested boundaries.</Note>

## Requirements

| Boundary | Requirement |
| --- | --- |
| Platform | macOS on Apple silicon |
| Repository | An on-disk Kotlin Gradle repository |
| IDE | IntelliJ IDEA in the `262` release line |
| Kotlin | The Kotlin plugin bundled with that IDEA installation |
| Runtime | The Java 25 JBR bundled with IDEA |
| Agent | A compatible Codex client |

<Steps>
  <Step title="Install the matched release">
    Run:

    ```shell
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/amichne/kast/main/install.sh)"
    ```

    The installer reports the detected IntelliJ version, verifies Kast and the
    matching IDEA plugin, registers a user-level Codex MCP server, and enables the app-server suite and its
    per-user macOS login LaunchAgent. The LaunchAgent keeps the Kast
    app server available across logins and restarts it after an unexpected exit.

    It does not install IDEA.

    If the Kast release has no plugin for the installed IDEA release line, the
    installer names the mismatch and installs nothing.

    The installer does not add a Kast command to `PATH`.
  </Step>
  <Step title="Prepare IDEA">
    - Restart IDEA to load the plugin.
    - Save edited files before requesting evidence.

    Kast opens the requested repository and waits for Gradle import and
    indexing when a semantic request arrives. Resolve project trust or unsaved-document blockers if the
    agent reports them; see [troubleshooting](/troubleshooting).
  </Step>
  <Step title="Connect from a Gradle repository">
    ```console
    cd /path/to/kotlin-repository
    codex
    ```

    Kast starts its MCP process for the session, discovers the exact Gradle
    root, and prepares the IDEA project on semantic demand. No worktree
    registration is needed.
  </Step>
  <Step title="Ask for a declaration">
    Ask: **“Use Kast to find the class OrderService.”** Replace `OrderService`
    with a class in your project.

    A successful request returns matches with names, locations, and reusable
    `ref` values. Read the semantic status: a qualified result has limits;
    a rejected request needs the reported problem resolved.

    Continue with [Search declarations](/search) or [Read a response](/reference/responses).
  </Step>
</Steps>

## Setup alternatives

<AccordionGroup>
  <Accordion title="Choose among multiple IDEA installations">
    ```shell
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/amichne/kast/main/install.sh)" \
      -- --idea-home "/Applications/IntelliJ IDEA.app"
    ```

    See [Compatibility](/reference/compatibility) for tested IDEA builds.
  </Accordion>
  <Accordion title="Uninstall Kast">
    ```shell
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/amichne/kast/main/install.sh)" \
      -- uninstall
    ```

    Uninstall removes Kast-owned links, installations, runtime state,
    configuration, services, and the Kast-owned Codex MCP registration.

    Repositories and unrelated JetBrains state remain.
  </Accordion>
</AccordionGroup>

## Next steps

- Continue with [Search declarations](/search) when the first request succeeds.
- Use [Troubleshooting](/troubleshooting) when tools are absent or a request rejects.
- Complete the broker-trust step in [Change source](/change) before a source change.

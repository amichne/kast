- Profiling harness issue: `wall,cpu` launched async-profiler modes concurrently against one JVM, and async-profiler rejected the second mode with `Profiler already started`.
- Clean workaround validated before the harness fix: run one async-profiler mode per profiling run (`wall` and `cpu` separately) to produce clean flame graph artifacts.
- Clean 30-module synthetic wall run: `.benchmarks/standalone-profile/results/local-synthetic-30-wall-20260526T1824`, ready in about 20.2s, flame graph `profiling/startup-workload-wall.html`.
- Clean 30-module synthetic CPU run: `.benchmarks/standalone-profile/results/local-synthetic-30-cpu-20260526T1825`, ready in about 10.2s, flame graph `profiling/startup-workload-cpu.html`.
- First bottleneck signals from 30-module synthetic runs: `raw/workspace-files`, `raw/diagnostics`, `raw/workspace-search`, and `raw/workspace-symbol` are consistently the slowest JSON-RPC operations after startup.
- The profiling config already enables kast telemetry with `scopes = "all"` and `outputFile = telemetry/standalone-spans.jsonl`; telemetry detail is currently `basic`.
- Existing telemetry detail parsing supports at least a non-basic/verbose path in the IntelliJ backend code; standalone config defaults to `basic`, so verbose support should be verified before relying on it for the Docker harness.
- Added RED contract test `.github/scripts/test-standalone-profile-harness-contract.sh`; current expected failure is that the runner lacks `run_profile_mode()`.
- Verified standalone telemetry has `StandaloneTelemetryDetail.VERBOSE`; verbose output includes span events in `standalone-spans-*.jsonl`.
- Harness change in progress: each requested async-profiler mode now runs sequentially with a fresh daemon, separate cache/config/log/diagnostic directories, verbose telemetry, JFR recordings, and per-mode RPC latency tagging.
- Fixed-harness 3-module validation run succeeded for combined `wall,cpu`: `.benchmarks/standalone-profile/results/local-harness-fix-3m-20260526T1900`.
- Fixed-harness 3-module run produced both flame graphs (`startup-workload-1-wall.html`, `startup-workload-2-cpu.html`), both JFR recordings, per-mode telemetry JSONL files, per-mode diagnostics, and RPC rows tagged with `profileMode`.
- In the 3-module fixed-harness run, `asprof-wall.exit` and `asprof-cpu.exit` were both `0`; the previous `Profiler already started` failure did not recur.
- Fixed-harness 30-module validation run succeeded for combined `wall,cpu`: `.benchmarks/standalone-profile/results/local-harness-fix-30m-20260526T1905`.
- In the 30-module run, both async-profiler exits were `0`; wall ready time was about 11.5s and CPU-mode ready time was about 3.2s, likely affected by warmed shared Gradle home despite per-mode kast cache/config directories.
- 30-module slowest RPCs stayed consistent: `raw/workspace-files`, `raw/diagnostics`, `raw/workspace-search`, and `raw/workspace-symbol` led the latency list.
- 30-module telemetry p95 signals: wall run `kast.workspaceFiles` about 1128ms, `kast.workspaceSearch` about 493ms, `kast.diagnostics` about 330ms; CPU run `kast.workspaceFiles` about 1377ms, `kast.workspaceSearch` about 416ms, `kast.diagnostics` about 385ms.
- 30-module JFR summary showed thousands of `org.jetbrains.kotlin.LLPhase` events plus heavy allocation pressure in `byte[]` (about 53-54%), with notable `String`/collection/Zip/Kotlin protobuf allocations.
- 30-module JFR GC summaries showed total pause time around 642-717ms over the whole recording window; lock telemetry remained tiny, mostly below 0.1ms per lock span.
- Verbose telemetry files were produced, but the current standalone spans had no span events attached; verbose still preserves the event field and is ready if spans start emitting events.
- Added the standalone profile harness contract to the CI workflow-contract job so the sequential profiler/JFR shape is checked alongside the manual profiling workflow contract.
- Updated the manual standalone profiling workflow summary to include `profileModes` and `profileRuns`, so wall and CPU startup/artifact data are visible separately in GitHub step summaries.
- Ktor 3.2.3 verification: exact tag exists at `dfbeb283641ddb74fd90c5fcbc9f0b12aa8dbaf1`, Gradle wrapper is `8.14.2`, local checkout count found 1,995 `.kt`, 152 `.kts`, 0 `.java`, 122 root settings DSL projects, and 130 `build.gradle.kts` files across root/included builds.
- Ktor 3.2.3 is representative of a large real Kotlin-heavy Gradle/KMP workspace, but it also exposes a current standalone discovery gap: Ktor mostly uses platform/source-set directories such as `posix/src`, `js/src`, `wasmJs/src`, etc., not standard `src/main/kotlin` module roots.
- Ktor profiling attempt `.benchmarks/standalone-profile/results/local-ktor-3.2.3-20260526T1915` failed before health with `No source roots were found under /work/target` in both wall and CPU daemon startups.
- Ktor attempt still produced useful early artifacts: wall async-profiler exit `0` with `profiling/startup-workload-1-wall.html`, JFR files for wall/cpu startup attempts, startup diagnostics, and stderr logs showing the source-root discovery failure.
- Ktor cold run also surfaced local environment pressure: Docker/Colima reported about `7.7 GiB` memory limit while Ktor's Gradle daemon starts with `-Xmx9g`; the attempt reached roughly `5.8-6.9 GiB` before the daemon failure path completed.
- Ktor wall JFR ran for about 508s during the timeout/failure path; top event counts were native method samples, native memory usage, Gradle/Kotlin bootstrap processes, and container resource events rather than successful kast RPC spans.
- Gradle Tooling API probe against Ktor 3.2.3 returned 125 IDEA modules but zero IDEA source directories for the first sampled modules, so `IdeaProject` alone is not enough for Kotlin multiplatform/source-set layouts.
- EclipseProject probing against Ktor likewise reported zero source directories/classpath entries for sampled modules, reinforcing that Kast needs a Gradle-owned source-set extraction path rather than another stock IDE model.
- Added RED tracer for Gradle-owned Kotlin-style source-set roots: `./gradlew :backend-standalone:test --tests 'io.github.amichne.kast.standalone.StandaloneWorkspaceDiscoveryTest.tooling api discovers Gradle-owned Kotlin source set roots when IDEA model omits them' --stacktrace`; it failed with empty `:app` mainSourceRoots.
- Added a fallback Gradle source-set extraction task through the Tooling API; the targeted tracer test now passes and returns `common/src` as main plus `jvmTest/src` as test when the IDEA model has no roots.
- Docker profiling harness now injects Gradle user-home `gradle.properties` with bounded Gradle/Kotlin daemon heaps and worker count; this is intended to override target repo defaults such as Ktor's `-Xmx9g` Gradle daemon on smaller Colima allocations.
- The host profiling wrapper now accepts `--docker-memory`, `--gradle-jvmargs`, `--kotlin-daemon-jvmargs`, and `--gradle-workers-max`; the manual GitHub workflow exposes the same Gradle heap settings.
- Added `--ready-timeout-seconds` to the profiling wrapper so large real repos can get a longer health/READY window without editing container scripts.
- First Ktor retry with bounded Gradle heap failed before source extraction because Ktor's Gradle Doctor requires `-XX:+UseParallelGC`; updated the harness Gradle JVM defaults to include it.
- Second Ktor retry showed Ktor's IDEA model has exactly one Java module with roots, which skipped the fallback; changed the gate to run Gradle source-set extraction whenever any non-root module lacks source roots, and extended the tracer fixture to include a Java module so this mixed case stays covered.
- Diagnostic Ktor retry proved the fallback ran across 125 modules but returned zero roots; root cause was checking Gradle `SourceDirectorySet` as `Iterable` before reading `srcDirs`, so sparse/empty configured dirs were discarded. Fixed extractor order and made the tracer source directory set iterable with no files.

# Unrelated observations

No unrelated product defect has been established or repaired in this change.

- The production plugin declares `262` through `262.*` compatibility. Exact SDK
  and native-harness inputs coexist with that range. The older experimental
  plugin template still declares an exact build; it was left unchanged.
- The private native IDEA startup logs a missing `node` mise shim and a missing
  `SHELL` environment variable. The focused compiler-backed ownership matrix
  still completed. Environment cleanup is outside this deliverable.
- Existing untracked `.codex/` in Kast and `.agent-turn/` in Konditional were
  preserved and excluded from the tracked source copies and commits.

# Results

`final.json` and `final.md` are the authoritative two-run Kast result and
canonical Markdown projection. `graphify-final.json` records the same-snapshot
Graphify answers, `comparison.json` applies the shared rubric, and
`native-graph-final/` contains the clean complete graph rebuild evidence.

`latest.json` remains an uncommitted convenience output of the default runner.

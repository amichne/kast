# `traversal` guide

This directory owns files and child directories under `traversal`. Follow [the nearest owner guide](../AGENTS.md) for boundaries, invariants, and verification.

## Local scope

- Keep changes within the parent guide's ownership.
- Add local rules only when this directory gains a distinct durable boundary.

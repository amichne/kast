---
title: "Kast"
description: "Compiler-grounded evidence for exact Kotlin repositories."
mode: "wide"
icon: "braces"
keywords: ["Kotlin", "compiler", "semantic analysis", "coding agents"]
---

Kast gives engineers and coding agents a repo-local command boundary for
questions whose answers depend on compiler identity, not a convincing reading
of source text. Every operation returns a closed status and preserves the
proof-specific root, generation, scope, limits, and identities needed to judge
its evidence.

<div className="flex flex-wrap gap-2"><Badge color="purple" icon="braces" shape="pill">11 typed operations</Badge> <Badge color="blue" icon="folder-key" shape="pill">Exact-root authority</Badge> <Badge color="green" icon="shield-check" shape="pill">Closed outcomes</Badge></div>

## The compiler sees more than text

Consider a checkout service that imports one of several declarations named
`PricePolicy`:

```kotlin
import billing.PricePolicy as BillingPricePolicy

class Checkout(private val policy: BillingPricePolicy) {
    fun total(order: Order): Money = policy.price(order)
}
```

<Columns cols={2}>
  <Card title="Text can suggest">
    A search can find `PricePolicy`, `policy`, and `price`. It cannot establish
    which declaration the alias names or which overload receives `order`.
  </Card>
  <Card title="Compiler evidence can establish">
    The property and call each have an exact compiler identity. Their source
    locations and semantic relationship remain part of the result.
  </Card>
</Columns>

The distinction becomes important when an answer crosses files, modules, or a
change boundary. Kast makes the strength and limits of that answer visible.

## Start from the repository root

```console
cd /path/to/kotlin-repository
kast start
```

`kast start` launches or reuses the exact-root private IntelliJ sidecar. It
returns when semantic evidence is ready, or returns a typed blocker instead of
manufacturing readiness.

<Card
  title="Set up and start Kast"
  icon="rocket"
  href="/start"
  cta="Open the quickstart"
  arrow
  horizontal
>
  Install the matched control and sidecar release, admit one repository, and
  inspect the first compiler-backed result.
</Card>

## Eleven sidecar operations

The installed sidecar endpoint publishes explicit index synchronization,
topology build, candidate discovery, compiler-backed symbol inspection, bounded
source reads, one-hop relation reads, bounded traversal, scoped diagnostics,
and planning, verified application, or recovery for AddDeclaration changes.

<Columns cols={2}>
  <Card title="What is Kast ready to inspect?" icon="gauge" href="/questions/workspace-readiness" cta="Inspect readiness" arrow>
    See the exact root, semantic generation, and readiness limits before
    relying on later answers.
  </Card>
  <Card title="What declaration is this?" icon="scan-search" href="/questions/declaration-identity" cta="Establish identity" arrow>
    Refine a bounded candidate or inspect an exact selector and receive exact
    compiler evidence in one operation.
  </Card>
  <Card title="How is this code connected?" icon="route" href="/questions/code-connections" cta="Trace connections" arrow>
    Read a live compiler relation directly, or publish exact-generation
    topology and traverse it with explicit depth and result limits.
  </Card>
  <Card title="How can I add a declaration safely?" icon="file-pen" href="/questions/safe-change" cta="Follow the lifecycle" arrow>
    Plan, apply with successor publication and verification, or recover one
    admitted AddDeclaration mutation without restarting the endpoint.
  </Card>
</Columns>

## Evidence keeps its boundary

<Info>
  **Complete** means the operation met its declared scope and completeness
  contract. **Qualified** keeps usable evidence and names its limits.
  **Rejected** returns no evidence as success.
</Info>

Every CLI outcome names its operation and status. Generation-sensitive
authority remains in the selectors, snapshots, plans, receipts, and evidence
documents that need it. A moved generation, unsupported scope, exhausted
bound, or missing capability stays visible at the relevant boundary.

[Learn how to judge an answer](/concepts/evidence-boundaries) or
[see how Kast works](/explanation/how-kast-works).

Repository: `amichne/kast` (branch: `main`)

IMPORTANT CONSTRAINT: Files under `docs/reference/` (`api-reference.md`, `capabilities.md`) are GENERATED by `./gradlew :analysis-api:generateDocPages` and drift-tested by `AnalysisDocsDocumentTest`. Do NOT hand-edit them. Changes to those pages require modifying the generation code. The plan below addresses this where relevant.

---

### 1. `docs/index.md` (REWRITE — most important page)

Current state: 490 lines. Simultaneously a value pitch, feature showcase (5 full tabbed examples with JSON), LSP comparison, backend explainer, quickstart, and nav hub.

**Replace the entire file with this structure (target: ~120 lines):**

**Section A: Frontmatter + hero (lines 1-25)**
- Keep the existing frontmatter (lines 1-5).
- Keep the H1 and the 3-sentence description paragraph (lines 8-15).
- Keep the mermaid flowchart (lines 17-26).

**Section B: "What makes Kast different" (NEW — ~20 lines)**
- Replace the entire "What can Kast do?" section (current lines 28-375, which is 5 full tabbed CLI+JSON-RPC+agent examples) with 4 short bullet points. Each bullet is ONE sentence + a link to the canonical capability page. No JSON. No code blocks. No tabs. The bullets are:
    1. **Symbol identity, not text matching** — Kast resolves the exact compiler declaration at a position and returns its fully qualified name, kind, and location. [Learn more →](what-can-kast-do/understand-symbols.md)
    2. **Exhaustive reference search** — Every reference result includes `searchScope.exhaustive`, proving whether every candidate file was searched. [Learn more →](what-can-kast-do/trace-usage.md)
    3. **Bounded call hierarchy** — Call trees include explicit depth, fan-out, and timeout limits with truncation metadata on every node. [Learn more →](what-can-kast-do/trace-usage.md#expand-the-call-hierarchy)
    4. **Safe mutations** — Rename uses a two-phase plan→apply flow with SHA-256 file hashes for conflict detection. [Learn more →](what-can-kast-do/refactor-safely.md)

**Section C: "See it on your code" (NEW — ~10 lines)**
- Add a new section promoting `kast demo`. This does NOT exist anywhere in the docs site currently — it only appears at line 153 of README.md. The section should contain:
    - A 2-sentence explanation: "kast demo runs an interactive comparison of grep-based text search versus Kast's semantic analysis on your own workspace. It picks a symbol, shows what grep gets wrong, then runs resolve, references, rename (dry-run), and call-hierarchy to show the difference."
    - The two commands from README.md lines 164-171:
      ```
      kast demo --workspace-root=/path/to/your/kotlin/project
      kast demo --workspace-root=/path/to/your/kotlin/project --symbol=YourClassName
      ```

**Section D: "Get running in 60 seconds" (MOVE UP — keep as-is)**
- Move the existing quickstart block (current lines 429-452) to immediately after the demo section. Keep it exactly as-is.

**Section E: Remove these sections entirely:**
- "Why not just use an LSP?" (lines 377-398) — replace with one sentence: "Kast isn't a replacement for your editor's LSP — it's the tool for when work happens outside an editor. [Full comparison →](architecture/kast-vs-lsp.md)"
- "Two backends, one protocol" (lines 400-427) — replace with one sentence: "Kast ships a standalone daemon and an IntelliJ plugin. Both speak the same protocol. [Learn more →](getting-started/backends.md)"

**Section F: "Next steps" cards (keep as-is)**
- Keep the existing card grid (lines 454-490) unchanged.

---

### 2. `README.md` (TRIM to ~40 lines)

Current state: 210 lines covering install, build, usage, demo, shell completion, and build-from-source.

**Keep:**
- Lines 1-2: Title + badges
- Lines 4-5: One-liner description ("Kast is a Kotlin analysis tool...")
- Lines 7-16: Repo module list (useful for contributors landing on GitHub)
- Lines 18-31: "Install the published CLI" section with the two install commands (curl + pipe)
- Add a NEW "Try it" section with the `kast demo` commands (from current lines 153-171)
- Add a link: "Full documentation → [docs site URL]"

**Remove everything else:**
- Lines 33-43: Component install variants (move to docs/getting-started/install.md if not already there — it IS already there at lines 46-73)
- Lines 45-65: Detailed install notes + skill install (already in docs/getting-started/install.md and docs/for-agents/install-the-skill.md)
- Lines 67-85: Local/dev builds (already in the repo for contributors who know to look)
- Lines 86-151: "How to use it" section (already in docs/getting-started/quickstart.md and docs/what-can-kast-do/manage-workspaces.md)
- Lines 174-210: Shell completion + build from source (already in docs/getting-started/install.md lines 96-111 and can stay discoverable via `kast --help`)

---

### 3. `docs/what-can-kast-do/understand-symbols.md` (MINOR EDIT)

Current state: 338 lines. This is the canonical home for resolve, outline, workspace-symbol, and implementations examples. Content is good and focused.

**Changes:**
- No structural changes needed. This page is already well-organized.
- The examples here are the CANONICAL versions. The duplicates on `docs/index.md` are being removed (see step 1 above), so no action needed here.

---

### 4. `docs/what-can-kast-do/trace-usage.md` (MINOR EDIT)

Current state: 405 lines. Canonical home for references, call-hierarchy, and type-hierarchy.

**Changes:**
- The "How truncation works" section (lines 241-281) duplicates `docs/architecture/behavioral-model.md` lines 35-78. Make THIS page the canonical home for the truncation explanation (it's where readers encounter it in context). In `behavioral-model.md` (step 10 below), replace the duplicate with a link here.
- No other changes needed.

---

### 5. `docs/what-can-kast-do/refactor-safely.md` (NO CHANGES)

Current state: 250 lines. Well-focused. Covers rename, apply-edits, optimize-imports with the plan→hash→apply flow. No duplication issues.

---

### 6. `docs/what-can-kast-do/validate-code.md` (NO CHANGES)

Current state: 172 lines. Focused. Covers diagnostics, code-actions, completions. No duplication issues.

---

### 7. `docs/what-can-kast-do/manage-workspaces.md` (NO CHANGES)

Current state: 172 lines. Focused. Covers daemon lifecycle, workspace discovery, refresh, files, capabilities, health. No duplication issues.

---

### 8. `docs/for-agents/index.md` (TRIM — second most important rewrite)

Current state: 100 lines. Re-explains symbol identity, structural evidence, safe mutations, and workspace awareness — all concepts already covered in `what-can-kast-do/` pages.

**Replace lines 34-74 (the four concept sections) with a shorter version:**

Each of the four sections ("Symbol identity", "Structural evidence", "Safe mutations", "Workspace awareness") should be reduced to:
- ONE sentence summarizing the capability
- A link to the canonical page in `what-can-kast-do/`

Keep:
- Lines 1-33: Frontmatter, intro paragraph, and mindmap diagram (these are good and unique to the agent context)
- Lines 76-100: "What your agent can do with Kast" bullet list and "Next steps" (these are good and unique)

The mindmap diagram (lines 16-32) is a good visual summary and should stay.

---

### 9. `docs/for-agents/talk-to-your-agent.md` (NO CHANGES)

Current state: 136 lines. Unique content about prompting patterns, golden path flow, disambiguation. No duplication.

---

### 10. `docs/for-agents/install-the-skill.md` (NO CHANGES)

Current state: 82 lines. Focused and unique.

---

### 11. `docs/for-agents/direct-cli.md` (NO CHANGES)

Current state: 122 lines. Focused and unique.

---

### 12. `docs/architecture/behavioral-model.md` (TRIM — deduplicate with capability pages)

Current state: 174 lines. Several sections duplicate content from capability pages.

**Changes:**

- **"Call hierarchy is intentionally bounded" (lines 35-78):** Replace the full explanation + mermaid diagram + table + bullet list with a 2-sentence summary and a link: "Call hierarchy results are bounded by configurable depth, fan-out, total edge, and timeout limits. Every truncated node carries a reason. [Full truncation model →](../what-can-kast-do/trace-usage.md#how-truncation-works)"

- **"Reference search is visibility-scoped" (lines 99-125):** Replace the full table + bullet list with a 2-sentence summary and a link: "Kast narrows reference searches based on Kotlin visibility rules and reports exhaustiveness via `searchScope`. [Full scoping model →](../what-can-kast-do/trace-usage.md#how-visibility-drives-scope)"

- **Keep these sections as-is** (they contain unique content not found elsewhere):
    - "Everything is workspace-scoped" (lines 15-33)
    - "Entry points have no incoming callers" (lines 80-86)
    - "Symbol resolution is position-based" (lines 87-98)
    - "File outline shows named declarations only" (lines 127-135)
    - "Workspace symbol search is name-based" (lines 137-150)
    - "Rename plans use hash-based conflict detection" (lines 152-158)
    - "The daemon refreshes automatically" (lines 160-168)

---

### 13. `docs/architecture/how-it-works.md` (NO CHANGES)

Current state: 194 lines. Unique architectural content. No duplication issues.

---

### 14. `docs/architecture/kast-vs-lsp.md` (NO CHANGES)

Current state: 114 lines. The inline duplicate on `docs/index.md` is being removed (step 1). This page becomes the single canonical home for the comparison.

---

### 15. `docs/reference/capabilities.md` (DELETE from nav)

Current state: 421 lines. This is a GENERATED file that is a condensed version of `api-reference.md` — same operations, same field tables, just with collapsible sections instead of full examples.

**Action:** Remove `reference/capabilities` from the navigation in `docs/docs.json` (line 65). The file can remain in the repo (it's generated), but it should not appear in the site navigation. Having both `api-reference.md` and `capabilities.md` in the nav forces readers to wonder which is authoritative.

If you want to keep it accessible, add a note at the top of `api-reference.md` saying "For a condensed schema-only view, see [Capabilities](capabilities.md)." But do NOT list both in the primary nav.

**In `docs/docs.json`, change line 65:**
Remove `"reference/capabilities"` from the `pages` array in the Reference group (lines 61-67).

---

### 16. `docs/reference/api-reference.md` (NO HAND EDITS — generated file)

This file is generated. No changes.

---

### 17. `docs/reference/api-specification.md` (NO CHANGES)

Current state: 72 lines. Short pointer to the OpenAPI YAML. Fine as-is.

---

### 18. `docs/reference/error-codes.md` (NO CHANGES)

Current state: 63 lines. Focused and unique.

---

### 19. `docs/troubleshooting.md` (NO CHANGES)

Current state: 173 lines. Well-organized with collapsible sections. No duplication.

---

### 20. `docs/changelog.md` (NO CHANGES)

Current state: 31 lines. Fine.

---

### 21. `docs/AGENTS.md` (UPDATE to reflect new structure)

After all changes are made, update the "Site structure" section (lines 8-20) to reflect:
- That `docs/index.md` is now a focused landing page (not a "sales-pitch landing page with demos")
- That `reference/capabilities` is no longer in the nav
- That `kast demo` is now documented on the landing page

---

### 22. `docs/docs.json` (UPDATE nav config)

In the "Reference" group (lines 60-67), remove `"reference/capabilities"` from the pages array. The result should be:
```json
{
  "group": "Reference",
  "pages": [
    "reference/api-reference",
    "reference/api-specification",
    "reference/error-codes"
  ]
}
```

No other nav changes needed — the page structure stays the same, just the content within pages changes.

---

### Summary of changes by type:

| Action              | Files                                                               |
| ------------------- | ------------------------------------------------------------------- |
| **Rewrite**         | `docs/index.md`, `README.md`                                        |
| **Trim**            | `docs/for-agents/index.md`, `docs/architecture/behavioral-model.md` |
| **Remove from nav** | `docs/reference/capabilities.md`                                    |
| **Update**          | `docs/docs.json`, `docs/AGENTS.md`                                  |
| **No changes**      | All 14 other files                                                  |

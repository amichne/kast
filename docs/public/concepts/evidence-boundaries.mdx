---
type: "Concept"
title: "Trust the evidence"
description: "Interpret complete, qualified, and rejected Kast outcomes within their exact root, generation, scope, and bounds."
resource: "file://docs/public/concepts/evidence-boundaries.mdx"
tags: ["evidence", "outcomes", "generation", "scope"]
timestamp: "2026-09-03T00:00:00-04:00"
code_sources:
  - path: "kernel/src/main/kotlin/io/github/amichne/kast/kernel/OperationOutcome.kt"
    lines: "3-22"
    symbols: ["OperationOutcome", "Complete", "Qualified", "Rejected"]
  - path: "protocol/contract/src/main/kotlin/io/github/amichne/kast/protocol/contract/CanonicalOperation.kt"
    lines: "7-20"
    symbols: ["CanonicalOperation"]
icon: "shield-check"
keywords: ["evidence", "complete", "qualified", "rejected", "generation"]
---

A successful transport only proves that one request crossed the local wire.
The semantic result has its own closed outcome and evidence coordinates.

## Three closed outcomes

| Outcome | What it proves |
| --- | --- |
| **Complete** | The operation met its admitted scope, bounds, and completeness policy. The payload can support the full claim described by that operation. |
| **Qualified** | The payload remains usable within a named limitation. Missing coverage or a reached bound stays attached to the evidence. |
| **Rejected** | Kast established no successful payload. The response returns one typed reason from the operation's finite failure set. |

Outcome support varies by operation:

- Discovery, relation, traversal, diagnostics, and manual recovery may return
  qualified evidence.
- Index synchronization, topology publication, exact symbol inspection,
  planning, and verified application require complete evidence for success.
- Workspace readiness is either complete or rejected on the installed
  exact-root route; its internal reconciling state is not published as ready.

The [CLI reference](/reference/cli) lists all eleven sidecar operations and
their command shapes.

## Evidence is valid at its coordinates

When an operation returns semantic evidence, judge it against the coordinates
carried by its payload and by the selector, snapshot, plan, or receipt that
authorized it:

<Steps>
  <Step title="Exact root">
    The canonical repository checkout that owns the admitted sidecar endpoint.
  </Step>
  <Step title="Operation">
    The typed question asked, such as `index.sync` or `symbol.inspect`.
  </Step>
  <Step title="Generation">
    The semantic workspace state against which selectors and evidence were
    issued.
  </Step>
  <Step title="Scope and bounds">
    The discovery mode, search scope, result limit, and completeness policy
    admitted for the request.
  </Step>
  <Step title="Qualification">
    Any explicit limitation that prevents the result from supporting a
    stronger claim.
  </Step>
</Steps>

<Info>
  Unknown, stale, unsupported, incomplete, or unproven coordinates fail closed.
  Kast does not guess a current symbol or turn partial coverage into complete
  evidence.
</Info>

## Judge a negative answer

An empty result is strong evidence only when the result is complete for the
scope that matters. Before treating "no matches" as a repository fact, check:

- the exact root is the intended checkout
- the generation is still current
- the selected operation answers the intended question
- scope and result limits cover the decision
- the outcome is complete, not qualified or rejected

[How Kast works](/explanation/how-kast-works) shows where their evidence is
admitted and projected.

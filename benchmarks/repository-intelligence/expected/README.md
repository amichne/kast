# Expected Results

`questions.jsonl` owns the independently derived machine assertions. This
directory receives stable aggregate expectations after the Phase 0 baseline is
captured and before product behavior changes.

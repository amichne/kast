# `workspace/intellij/src` guide

This directory owns source-set directories under `workspace/intellij/src`. Follow [the nearest owner guide](../AGENTS.md) for boundaries, invariants, and verification.

## Local scope

- Keep changes within the parent guide's ownership.
- Add local rules only when this directory gains a distinct durable boundary.

# Packaged Kast skill guide

The packaged skill teaches compiler-backed Kotlin and Gradle command discovery and synchronous mutation results. It must not describe session hooks, task gates, or persisted lifecycle state.

# `build-logic/src/test/kotlin/support/architecture/projection` guide

This directory owns test sources and fixtures under `build-logic/src/test/kotlin/support/architecture/projection`. Follow [the nearest owner guide](../../../../../../AGENTS.md) for boundaries, invariants, and verification.

## Local scope

- Keep changes within the parent guide's ownership.
- Add local rules only when this directory gains a distinct durable boundary.

# `change/apply/src/main` guide

This directory owns production sources under `change/apply/src/main`. Follow [the nearest owner guide](../../AGENTS.md) for boundaries, invariants, and verification.

## Local scope

- Keep changes within the parent guide's ownership.
- Add local rules only when this directory gains a distinct durable boundary.

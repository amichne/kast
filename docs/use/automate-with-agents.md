# Automate with agents

Use `kast agent` and scoped `--help` to discover compiler-backed Kotlin and Gradle operations.
Keep automation on the public command dialect.

Mutations execute synchronously. Exit code 0 means Kast planned and applied the write and diagnostics are green for the resulting contents. Failures return a compact structured code, message, and actionable details.

Agent commands default to TOON.

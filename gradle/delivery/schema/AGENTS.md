# Delivery JSON Schemas

These Draft 2020-12 schemas constrain the program projection, requirement trace, and future proof receipts. Keep each schema closed where the document shape is known. A schema change is a delivery-contract change and must be paired with typed Kotlin authority, regenerated projections, and dependency-free validation through `scripts/verify_bundle.py`.

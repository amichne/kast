Deliver Kast’s `BestCaseVfsPassiveReusedIndex` state: the installed `kast` CLI must execute `workspace.inspect -> symbol.discover -> symbol.resolve -> symbol.describe` through a compatible exact-root endpoint hosted by the already-running supported IntelliJ IDE, using its existing `Project`, VFS, indexes, PSI, and semantic APIs. Kast remains the authority for typed requests, scope, exact identity, bounds, evidence, outcomes, and proof. The terminal path must create no second `Project`, Gradle import, VFS refresh, recursive repository walk, source hash inside a read, blocking read action, semantic work on the EDT, runtime archive read, private IDEA home, `kast-indexer` process, Kast-caused indexing cycle, or automatic fallback.

At program start, inspect the current `amichne/kast` head and bind the requirement, graph, commands, and source artifacts to that revision. Treat the canonical Kotlin delivery graph as the sole delivery authority; Gradle is the progression and enforcement engine; JSON is a deterministic projection only. Resolve contradictions, obsolete assumptions, missing prerequisites, and unproven claims before product work.

Work exactly one ready atomic task at a time. Before editing, prove all dependency receipts are valid at the same head and materialize the task contract: goal, dependencies, allowed reads, allowed writes, inputs, outputs, public interface, internal implementation, authority, effect, cost, forbidden work, RED command, GREEN command, review boundary, and completion receipt. Do not broaden the goal, absorb adjacent work, write outside allowed paths, perform forbidden work, add a bypass, fallback, compatibility path, second authority, or weaker gate. Capture the required RED failure before the change, then the GREEN proof, then emit the completion receipt. Each receipt must bind the program fingerprint, requirement fingerprint, exact head, dependency receipt digests, input digest, command digest, observed proof, and artifact digests. Any changed commit, requirement, dependency, command, input, or artifact invalidates the receipt.

Preserve proof through every boundary: `raw -> parsed -> admitted -> capability -> verified output`. Use distinct types for identity, generation, scope, authority, effect, cost, budget, evidence, continuation, publication, recovery, and verification. Use only `Complete`, `Qualified`, or `Rejected`. Assign one authority to each fact and one module to each privileged effect. A read capability must not recover import, refresh, process start, persistence, graph build, or source-write authority from a project object, runtime, backend, database, service locator, or generic context.

Implement the path as: parsed CLI command -> canonical root -> parsed endpoint descriptor -> compatible IDE endpoint -> single-flight project read permit -> before-epoch capture -> cancellable smart read -> detached result -> after-epoch capture -> equal-epoch revalidation -> closed outcome. IDE-observed movement advances the epoch and invalidates the read; it never triggers repair. Extend the existing architecture firewall so the default plugin/read graph cannot compile forbidden effects or depend on isolated runtime, mutation, topology, SQLite, indexer, or managed-distribution implementations.

Measure progress only through accepted receipts. Declare completion only when every dependency and gate passes at one exact head; clean-checkout and clean-user-home installed acceptance pass; event-storm, contention, stale-epoch, failure, corruption, and deterministic replay cases pass; exact-head CI passes; the full diff is independently reviewed; every valid finding is resolved; all gates are rerun after the final edit; every requirement has `PASS`; and the terminal receipt derives `BestCaseVfsPassiveReusedIndex`. If an external action is unavailable, finish all independent tasks, preserve a clean branch, identify the exact blocked gates and required action, and do not claim completion.

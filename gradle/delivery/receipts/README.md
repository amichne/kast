# Proof receipts

This directory is empty until Gradle records gate receipts. A receipt is evidence, not status. It is admitted only when its program fingerprint, exact head, dependency receipt digests, declared input digest, command digest, observations, artifact digests, and own digest all match the canonical program.

Do not edit receipts. A changed repository head, requirement, command, dependency, input, or artifact invalidates the receipt.

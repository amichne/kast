# Proof-boundary verification helpers

This directory owns dependency-free Python helpers shared by the public bundle verifier.
Keep closed document validation, historical receipt admission, and focused report checks here;
the top-level scripts remain the executable public boundaries.

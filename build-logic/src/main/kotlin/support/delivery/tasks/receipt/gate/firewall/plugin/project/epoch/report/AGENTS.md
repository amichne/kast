# KVP-017 report boundary

This directory owns the canonical generated KVP-017 read-epoch document, its closed admission
failure set, and deterministic negative mutation proof.

- Keep exact IDEA build 262.9437.185, the five ordered signals, three comparison relations, all
  finite observation failures, thirteen ordered cases, and canonical JSON bytes.
- Bind the production limits: 4,096 VFS events, 4,096 path characters, 8,192 path UTF-8 bytes, and
  16 cached Gradle models.
- Every forbidden contract/effect count, including repository walk and VFS traversal separately,
  must be zero. Each finite admission failure needs one deterministic mutation fixture.

---
type: "Documentation Index"
title: "Kast"
description: "Compiler-grounded Kotlin search for coding agents."
resource: "file://docs/public/index.mdx"
tags: ["Kotlin", "compiler", "agents", "search"]
timestamp: "2026-09-07T00:00:00-04:00"
code_sources:
  - path: "protocol/registry/src/main/kotlin/io/github/amichne/kast/protocol/registry/CanonicalAgentToolDefinitions.kt"
    symbols: ["CanonicalAgentToolDefinitions"]
  - path: "cli/src/main/kotlin/io/github/amichne/kast/cli/broker/provider/KastProvider.kt"
    symbols: ["KastProviderQualifier", "KastRuntime"]
mode: "wide"
icon: "scan-search"
keywords: ["Kotlin", "compiler", "coding agents", "semantic search"]
---

Kast gives coding agents compiler-grounded search over one exact Kotlin
repository. Developers install it and connect a harness; the agent asks the
questions and Kast manages the runtime and intermediate evidence.

## Start with the agent

<CardGroup cols={2}>
  <Card title="Search with Kast" icon="scan-search" href="/search">
    See how the agent discovers exact Kotlin declarations and relationships.
  </Card>
  <Card title="Install and connect" icon="rocket" href="/start">
    Install the matched release and connect the included Codex integration.
  </Card>
  <Card title="Integrate another harness" icon="bot" href="/agent-harnesses">
    Consume the exact installed tool projection without copying Kast's CLI contract.
  </Card>
  <Card title="Troubleshoot a failure" icon="life-buoy" href="/troubleshooting">
    Read the reported boundary, collect passive status, and choose a bounded recovery.
  </Card>
  <Card title="Judge an answer" icon="shield-check" href="/concepts/evidence-boundaries">
    Understand complete, qualified, and rejected semantic outcomes.
  </Card>
</CardGroup>

## Search is the main surface

The agent-facing `kast.query` tool searches declarations, filters results, and
expands semantic relationships in one typed request. Its `symbols` source
establishes exact compiler identity, so the agent does not need to coordinate a
manual discovery-and-inspection sequence.

Specialist tools remain available for narrower contracts, but they are deferred.
The harness-provided selection policy tells the agent to start with search and
preserve returned references instead of reconstructing identities from text.

## Kast owns readiness

An agent does not need to start Kast, synchronize the repository, or build a
topology before asking a semantic question. Every operation starts or joins the
exact-root runtime and acquires its own prerequisites.

Kast returns a typed limitation or rejection when it cannot establish current
evidence. It does not manufacture readiness or silently weaken a result.

## The compiler resolves what text cannot

```kotlin
import billing.PricePolicy as BillingPricePolicy

class Checkout(private val policy: BillingPricePolicy) {
    fun total(order: Order): Money = policy.price(order)
}
```

Text search can find `PricePolicy`, `policy`, and `price`. Compiler evidence can
establish which declaration the alias names and which overload receives
`order`, while retaining the repository root, semantic generation, scope, and
limits behind the answer.

<Info>
  The human-facing contract is intentionally small. Harness authors can read the
  exact installed schemas and invocation bindings from `serverProjection` when
  they need to integrate Kast.
</Info>

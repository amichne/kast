# Documentation tooling

This subtree owns locked, build-only tools used to validate or generate public documentation.
Each tool directory must commit its complete dependency authority and expose one repository-level
check. Tool installations and generated caches remain ignored build state, not public-site input.

Follow `docs/AGENTS.md` for the public source and generated-asset boundary.

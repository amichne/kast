# Staging candidates

Files in this directory are **not part of the canonical benchmark**. They are history-derived task shapes that lack
durable oracle-backed grading and are not included in any benchmark run, paired analysis, or executive summary.

Do not cite pass rates or outcomes from staging candidates as benchmark evidence. Promote a candidate into
`evaluation/catalog.json` only after adding stable oracle surfaces per the instructions in `evaluation/README.md`.

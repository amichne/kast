# KVP-014 existing-Project receipt guide

This directory owns the deterministic KVP-014 report and typed exact-head receipt progression. Its
`epoch/` child owns KVP-015 signal characterization and directly re-admits this complete closure.

- The report is a closed generated-serialization document. Admission requires canonical bytes,
  the exact build-262 identities, the exact ready observation states, and zero for every forbidden
  stronger-effect counter.
- Execute only the declared negative and positive test selectors as fixed argument vectors.
- Reconstruct KVP-009 and KVP-012 independently at the current head before issuing either gate.
- RED binds the negative test source. GREEN binds the report, positive test source, and module
  build boundary. Completion consumes both gate receipts and both predecessor completions.

Raw JSON, process, source, and receipt bytes stay at their Gradle boundaries. Expected failures
remain finite typed data until a task renders them.

interface:
  display_name: "Kast"
  short_description: "IDE-grade Kotlin semantic analysis via `kast skill` subcommands"
  default_prompt: >
    Use $kast to understand Kotlin code, trace usages and callers, rename
    symbols safely, and validate edits with native `kast skill` subcommands.

policy:
  allow_implicit_invocation: true

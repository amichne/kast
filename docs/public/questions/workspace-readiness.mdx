---
title: "What is Kast ready to inspect?"
sidebarTitle: "Inspect readiness"
description: "Read exact-root workspace readiness before relying on sidecar symbol evidence."
icon: "gauge"
keywords: ["workspace", "readiness", "generation", "exact root"]
---

A running process is not proof that the repository model and compiler evidence
are ready. Ask for workspace evidence before interpreting a later symbol
result.

```console
cd /path/to/kotlin-repository
kast workspace inspect
```

## The answer belongs to one exact root

Kast canonicalizes the working directory and associates the request with the
private sidecar Project for that exact root. Two checkouts of the same Git
repository are two workspace identities. A user's foreground IntelliJ window
does not select the workspace, and unsaved editor buffers are outside Kast's
on-disk authority.

This prevents an answer from silently borrowing indexes, Gradle ownership, or
source state from another checkout.

The admitted model may include a declared local source directory that does not
currently exist, such as an empty resources or generated-source root. Kast
preserves that IntelliJ-owned root identity without inventing source content;
topology enumeration simply has no candidates beneath it until it appears.
Invalid protocols, malformed paths, and roots outside the exact workspace are
still rejected.

## Read readiness as evidence

The installed route returns complete evidence only when the exact root has a
published ready workspace. Every other installed state closes as one of two
typed rejections.

<Tabs>
  <Tab title="Ready" icon="circle-check">
    ```json
    {
      "operation": "workspace.inspect",
      "status": "complete",
      "canonicalRoot": "/path/to/kotlin-repository",
      "state": "ready"
    }
    ```

    The root and ready state are established. Later operations can now issue
    generation-bound selectors and snapshots.
  </Tab>

  <Tab title="Root unavailable" icon="circle-dashed">
    ```json
    {
      "operation": "workspace.inspect",
      "status": "rejected",
      "reason": "root-unavailable"
    }
    ```

    Kast cannot currently establish a ready workspace for this exact root.
    Starting, reconciling, stopping, absent, and mismatched-root states remain
    failures at the installed route.
  </Tab>

  <Tab title="Runtime blocked" icon="shield-check">
    ```json
    {
      "operation": "workspace.inspect",
      "status": "rejected",
      "reason": "runtime-blocked"
    }
    ```

    Workspace publication reached a typed blocker. Inspect the lifecycle and
    repair the blocker before relying on semantic operations.
  </Tab>
</Tabs>

<Note>
  The current CLI projection does not include a `generation` field in the
  `workspace.inspect` document. Use this operation to establish the ready root,
  then preserve the generation-bound selectors, snapshots, plans, and receipts
  returned by operations that issue them.
</Note>

<Note>
  `kast status` passively reports whether the exact sidecar is running and
  whether its private cache is fresh, seeded, refreshing, smart, or requires a
  rebuild. `kast workspace inspect` may start the sidecar and answers whether
  semantic workspace evidence is available. Keep those questions separate.
</Note>

## Carry the generation forward

Candidates and exact selectors are issued against a semantic generation. If
the workspace moves, Kast rejects stale proof instead of treating an old
identity as current.

Restarting the sidecar also advances the internal generation conservatively.
Kast cannot prove source content stayed unchanged while the Project was closed
from the bounded workspace model alone. Rebuild topology to verify and, when
the exact candidates match, rebind the durable snapshot to the new generation.

Once readiness is sufficient, [establish the exact declaration](/questions/declaration-identity)
needed by the next question.

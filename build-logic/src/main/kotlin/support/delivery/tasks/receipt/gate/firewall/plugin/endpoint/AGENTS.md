# KVP-013 endpoint descriptor receipt guide

This directory owns KVP-013's exact-head endpoint-descriptor receipt progression.

- Execute only the declared descriptor negative-test command and the descriptor-report plus
  positive-test command as fixed Gradle argument vectors.
- Admit the complete KVP-005 and KVP-012 closures independently before issuing either gate
  receipt.
- Decode the generated descriptor report as a canonical closed 14-field document, refine its
  bounded path and numeric identities, and require valid compatibility syntax and exact capability
  order. KVP-012 remains independently admitted predecessor proof, not the fixture tuple.
- Bind the negative and positive test authorities, checked-in endpoint schema, and generated
  descriptor report bytes to their owning receipts.

Raw JSON, paths, process arguments, and Gradle properties may leave only at their named outer
boundaries. Keep every expected command, descriptor, and receipt failure finite typed data.
